Automobile : A Mobile Native app test framework.

1. Step to package and install in distributable format

$ mvn install

2. Step to run tests

$ mvn exec:java -pl automobile-driver -DtestFile=sample-test.xml
